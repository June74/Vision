# SB-20260812-121549 — Shared worktree Git index lock denied again

- **Status:** contained
- **Detected:** 2026-08-12T12:15:49Z
- **Area:** Phase B CI tail-supervisor diagnosis publication
- **Evidence:** `git add` and `git commit` could not create the shared worktree index lock under the parent repository's `.git\\worktrees\\phase-b-foundation` directory because Windows denied permission.
- **Impact:** No files or provider state changed; the intended commit was not created.
- **Resolution:** Retry the same narrowly scoped staging and commit operation with the approved elevated filesystem boundary after verifying the exact worktree and target paths.
- **Prevention:** Treat shared-worktree Git writes as an elevated operation on this Windows checkout and never broaden the target beyond the named worktree.

## Recurrence - 2026-08-16

At 2026-08-16T15:14:26Z, a narrowly scoped `git add` from the Phase C
worktree again could not create the shared worktree `index.lock`. No files
were staged and no application, provider, secret, database, or deployment
state changed. The exact worktree and file targets remain scoped; the next
retry uses the approved elevated filesystem boundary.
