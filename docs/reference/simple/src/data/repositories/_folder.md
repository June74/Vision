# `src/data/repositories`

Repositories translate Vision's shared domain objects into database rows. The
Phase C calendar-write repository keeps the approval envelope encrypted and
keeps the provider execution ledger owner-scoped.
