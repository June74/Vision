# `src/audit`

This folder accepts only a small set of operational facts with opaque IDs. It rejects titles, descriptions, notes,
attendees, credentials, uploaded text, locations, links, hidden properties, and nested payloads before serialization.
