# `src/client/calendar/`

This folder shows synchronized calendar events as a read-only chronology, lets
the owner correct Vision's private category, and now contains one guarded
one-off event loop. The composer requires a server preview and deliberate
confirmation; it does not expose general edit, move, cancel, or delete tools.
