# `src/client/calendar/`

This folder shows synchronized calendar events as a read-only chronology and lets the owner correct Vision's private category. It does not contain Google Calendar write controls.
