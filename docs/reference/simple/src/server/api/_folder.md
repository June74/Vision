# Server API routes

Contains authenticated application APIs that are not part of sign-in itself.
The Phase C write routes are the only user-facing event-write entry point and
require preview, explicit confirmation, verified status, and compensating undo.
