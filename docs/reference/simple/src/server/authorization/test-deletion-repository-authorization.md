# `src/server/authorization/test-deletion-repository-authorization.ts`

This Vitest-only helper creates safe test permissions. Production source must not import it.

## `createTestDeletionRepositoryAccess`

Creates one owner permission for a test.

## `createTestDeletionPurgeAccess`

Creates one system purge permission for a test.
