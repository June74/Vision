# `index.html`

The Vite HTML entry point establishes the document language, viewport, safe static description, page title, and `#root` mount element. It loads `src/client/main.tsx` as an ES module. It contains no runtime configuration or secret value.
