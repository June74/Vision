# `src/client/styles.css`

This stylesheet defines the root palette and minimal responsive shell layout. It is static browser code: it accepts no input, makes no requests, and contains no protected values. It is loaded only by `src/client/main.tsx`.
