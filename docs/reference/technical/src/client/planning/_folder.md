# `src/client/planning`

Browser adapters and React surfaces for the authenticated planning routes.
Response parsers require deterministic/template automation markers and the
approval-only calendar boundary before data reaches the UI.
