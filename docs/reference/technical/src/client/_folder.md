# `src/client`

The client folder is the React single-page application entry point. It uses public session/setup responses plus a CSRF value, and must not receive Worker bindings, provider credentials, database credentials, refresh tokens, or encryption keys.
