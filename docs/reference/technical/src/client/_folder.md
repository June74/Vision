# `src/client`

The client folder is the React single-page application entry point. It must not receive Worker bindings, provider credentials, database credentials, refresh tokens, or encryption keys.
