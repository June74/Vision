# `src/client/App.tsx`

## `App`

**Signature:** `App(): JSX.Element`

`App` is a pure React component with no inputs, side effects, network calls, or provider dependencies. It renders the required `Vision` heading and `Foundation status` text. The browser smoke test introduced in the next runtime task covers those visible labels.
