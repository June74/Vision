# `src/client/calendar/`

This presentation boundary consumes only authenticated, allowlisted event display fields. It renders a bounded semantic list and submits explicit category corrections to Vision. Create, edit, move, cancel, and delete operations are intentionally absent because connected event writes belong to Phase C.
