# Google integration

This boundary owns fixed Google endpoints, bounded response parsing, PKCE parameters, and RS256 JSON Web Key Set (JWKS) verification. It does not create Vision sessions or apply the account allowlist.
