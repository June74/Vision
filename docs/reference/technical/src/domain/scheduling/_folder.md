# `src/domain/scheduling`

Pure scheduling contract. It validates busy blocks, converts UTC instants for
the requested IANA zone, returns deterministic alternatives, and exposes no
direct provider mutation.
