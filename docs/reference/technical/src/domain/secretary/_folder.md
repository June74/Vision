# `src/domain/secretary`

Pure deterministic local-secretary contracts. Inputs are bounded, timestamps
retain explicit offsets, timezone projection is IANA-validated, task transitions
are explicit, and calendar candidates can only become pending approval markers.
