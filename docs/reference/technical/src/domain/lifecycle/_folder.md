# Lifecycle domain rules

This folder owns provider-independent retention calculations. Persistence adapters consume these rules so the thirty-day UTC boundary remains consistent across requests and scheduled jobs.
