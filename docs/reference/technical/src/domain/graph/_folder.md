# `src/domain/graph`

This folder defines the common node envelope and governed edge registry. Edge validation verifies closed relation families, exact endpoint identities and types, a shared owner, and edge privacy at least as protective as both endpoints.
