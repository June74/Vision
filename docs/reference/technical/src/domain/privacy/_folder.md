# `src/domain/privacy`

This folder owns the closed privacy ordering and inference guard. It does not model a sharing approval workflow; all outputs explicitly deny sharing authorization so that authorization remains a separate policy boundary.
