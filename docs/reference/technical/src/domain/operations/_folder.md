# `src/domain/operations`

This folder contains provider-independent operational policy. It imports no database, Worker, Google, storage, or UI implementation and exposes only deterministic status calculations over validated content-free facts.
