# `src/domain/briefings`

Pure briefing assembly over trusted planning facts. The domain owns window
filtering, deterministic ordering, source citations, template automation, and
the approval-only calendar marker.
