# `src/jobs/phase-b-foundation-probe.ts`

Owns the exact `vision.phase-b-foundation-probe/v1` terminal evidence contract
and has only an injected aggregate source dependency.

## `createPhaseBFoundationProbeEvidence`

Admits every numeric field as a nonnegative safe integer, zeroes invalid
numeric inputs, and constructs the exact 19-key record with deterministic
semantic precedence.

## `runPhaseBFoundationProbe`

Requires exactly `{ VISION_ENV: "preview" }` and a finite `Date`, invokes the
source once, and maps allowlisted or unknown exceptions to canonical
unavailable evidence without copying error text.

## `emitPhaseBFoundationProbeEvidence`

Emits exactly `{ action: "acceptance.phase-b-foundation", evidence }`.

## `semanticCategory`

Evaluates role, schema, privileges, public grants, invariant counts, protected
storage, sentinel, and backup contract in interface order.

## `unavailableEvidence`

Returns the sole frozen zeroed shape for configuration, provider, or numeric
source failure.

## `isExactPreviewEnvironment`

Uses descriptors to require one enumerable data property and avoids reading
hostile accessors.

## `isValidDate`

Uses the intrinsic date getter and requires a finite timestamp.
