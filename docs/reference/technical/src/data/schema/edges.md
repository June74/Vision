# `src/data/schema/edges.ts`

Edges are contextual facts, never access grants. Owner-scoped foreign keys and reviewed relation/type checks govern their endpoints.

## `edges`

Defines the edge shape with relation, origin, lifecycle, privacy, validity, confidence, and version fields.
