# `src/server/webhooks`

This folder contains unauthenticated provider entry points. Each route must validate bounded headers and stored channel credentials before producing only opaque background work.
