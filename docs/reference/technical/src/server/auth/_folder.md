# Server authentication

Authentication routes own OAuth transaction state, callback validation, account allowlisting, session rotation, cookie policy, and CSRF enforcement. Provider tokens and protocol secrets cross only encrypted repository boundaries.
