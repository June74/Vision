# `src/server`

Server modules coordinate Worker requests and must keep provider and storage credentials out of client bundles. The initial boundary contains only deploy-time environment validation and typed static-asset access.
