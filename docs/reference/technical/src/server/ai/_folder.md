# `src/server/ai`

Server AI adapters sit after authentication and durable budget admission. They acquire protected records through verified repository capabilities and apply operation-specific disclosure policy before any provider call.
